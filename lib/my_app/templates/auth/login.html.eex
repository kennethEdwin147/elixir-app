<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ValoRank News - Login</title>

    <style>

    </style>
</head>
<body>

<div class="container">
    <form action="/login" method="post">

        <div class="field">
            <button type="button" style="background:none; color:blue; width:auto; padding:0; font-weight:normal; text-decoration: underline;">change</button>
        </div>

        <div class="field">
            <label for="email">Email</label>
            <input
                id="email"
                name="email"
                type="email"
                placeholder="e.g. julie.young@hey.com"
                required
                value="<%= @email_value %>" />
        </div>

        <div class="field">
            <label for="password">Password</label>
            <input
                id="password"
                name="password"
                type="password"
                placeholder="Enter your password"
                required />
        </div>

        <%= if @error_msg do %>
            <p class="error"><%= @error_msg %></p>
        <% end %>

        <div class="links">
            <a href="#">Reset password?</a>
        </div>

        <button type="submit">Continue</button>

        <div class="links" style="text-align: center;">
            <a href="/register">Don’t have an account yet? Sign up</a>
        </div>

    </form>
</div>

</body>
</html>
