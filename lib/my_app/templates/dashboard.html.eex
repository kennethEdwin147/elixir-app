<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Link in Bio</title>
</head>
<body>
    <nav>
        <a href="/dashboard">Dashboard</a> |
        <a href="/user/settings">Paramètres</a> |
        <form method="post" action="/login/logout" style="display: inline;">
            <button type="submit">Déconnexion</button>
        </form>
    </nav>

    <hr>

    <h1>Dashboard</h1>

    <p>Connecté en tant que: <strong><%= @user_email %></strong></p>

    <section>
        <h2>Votre profil public</h2>
        <%= if @profile_url do %>
            <p>Votre page: <a href="<%= @profile_url %>" target="_blank"><%= @profile_url %></a></p>
        <% else %>
            <p>⚠️ Configurez votre nom d'utilisateur dans <a href="/user/settings">Paramètres</a> pour activer votre page publique.</p>
        <% end %>

        <p>Bio: <%= if @bio != "", do: @bio, else: "Aucune bio" %></p>
    </section>

    <hr>

    <section>
        <h2>Vos liens (<%= @total_links %>)</h2>
        <p>Clics totaux: <%= @total_clicks %></p>

        <%= if length(@links) > 0 do %>
            <ul>
                <%= for link <- @links do %>
                    <li>
                        <strong><%= link.title %></strong> -
                        <a href="<%= link.url %>" target="_blank"><%= link.url %></a>
                        <br>
                        <small>Clics: <%= link.clicks %> | Position: <%= link.position %> | Actif: <%= link.active %></small>
                    </li>
                <% end %>
            </ul>
        <% else %>
            <p>Aucun lien pour le moment.</p>
        <% end %>

        <h3>Ajouter un lien</h3>
        <form method="post" action="/user/links">
            <div>
                <label>Titre:</label>
                <input type="text" name="title" required>
            </div>
            <div>
                <label>URL:</label>
                <input type="url" name="url" placeholder="https://..." required>
            </div>
            <div>
                <label>Icône (optionnel):</label>
                <input type="text" name="icon" placeholder="🔗">
            </div>
            <button type="submit">Ajouter</button>
        </form>
    </section>

    <hr>

    <footer>
        <p><a href="/user/settings">Modifier votre profil et apparence</a></p>
    </footer>
</body>
</html>
